# Commit Report

## Summary

- Project: `/home/beou/IdeaProjects/github-copilot-svcs`
- Branch: `x51-commit/20260604-174110-codex-provider-pr`
- Mode: single project
- Timestamp: `2026-06-04 17:42:32 +07`
- Branch decision: new branch, because the change set includes auth/security behavior, OpenSpec artifacts, and untracked documentation/workflow files; pushing on `main` would be unsafe because `main` ships to production.
- Commit title: `feat(codex): complete codex provider support`
- Push target: `origin x51-commit/20260604-174110-codex-provider-pr`

## Uncommitted Audit

- Codex provider/auth/runtime: complete Codex support beside Copilot, prefer official Codex auth store, sanitize status/logging, and fix explicit-model routing behavior.
- Tests and workflow docs: add regression coverage, add `AGENTS.md`, and include OpenSpec change artifacts for `add-codex-provider`.
- Formatting-only files: `src/auth.go`, `src/config_test.go`, and `src/transform_test.go` were touched by `make fmt` only.

## Files Planned For Commit

- `src/auth.go`: gofmt-only formatting change.
- `src/cli.go`: Codex auth/status/refresh behavior now uses official store and hides sensitive details.
- `src/codex_auth.go`: credential-source resolution, official-store expiry persistence/loading, and sanitized logging.
- `src/codex_provider.go`: runtime auth resolution aligned with auth mode and official-store fallback.
- `src/config_test.go`: gofmt-only formatting change.
- `src/integration_test.go`: regression tests for auth-source precedence, official-store fallback, routing ambiguity, capability mismatch, and model visibility.
- `src/router.go`: explicit-model capability mismatch now errors instead of falling back silently.
- `src/transform_test.go`: gofmt-only formatting change.
- `AGENTS.md`: repo-specific agent instructions and exact build/test/runtime/deploy guidance.
- `openspec/config.yaml`: OpenSpec config tracked with the change artifacts.
- `openspec/changes/add-codex-provider/.openspec.yaml`: change scaffold metadata.
- `openspec/changes/add-codex-provider/proposal.md`: proposal for Codex provider support.
- `openspec/changes/add-codex-provider/design.md`: implementation design for auth/transport/routing split.
- `openspec/changes/add-codex-provider/specs/codex-provider-support/spec.md`: normative Codex provider requirements.
- `openspec/changes/add-codex-provider/specs/multi-provider-routing/spec.md`: normative routing/model exposure requirements.
- `openspec/changes/add-codex-provider/tasks.md`: completed task checklist for the implemented change.

## Detailed Changes

- `src/cli.go`: `auth codex` and manual token flows now persist ChatGPT-backed credentials to the official Codex auth store, clear persisted secrets from proxy config, print operator-safe status fields only, and refresh through the resolved credential source.
- `src/codex_auth.go`: introduced explicit credential-source helpers for API-key and ChatGPT-backed modes, preserved `expires_at` in the official auth store, and removed raw account ID values from logs.
- `src/codex_provider.go`: API-key mode resolves keys from env/config/official store; ChatGPT mode resolves from official store before falling back; runtime logs no longer emit account identifiers.
- `src/router.go`: fixed a routing bug where an explicitly requested model could fall back to the default provider even when that model did not support the requested capability.
- `src/integration_test.go`: added regression tests covering official-store precedence, config fallback, ambiguity handling, capability mismatch, and merged model-map visibility.
- `AGENTS.md`: documented repo layout, exact `make fmt && make vet && make test && make build` flow, runtime config location, Codex/Copilot routing quirks, and production-impacting deploy behavior.
- `openspec/changes/add-codex-provider/*`: tracked the full OpenSpec proposal/design/spec/tasks set corresponding to the implemented Codex provider work.

## Change Content Summary

- Behavior: Codex now behaves as a first-class provider beside Copilot, with correct auth-source selection and safer routing behavior.
- Config: ChatGPT-backed Codex secrets are no longer stored in proxy config; the official Codex auth store is the primary source.
- Docs: added repo agent guidance and committed OpenSpec artifacts for the change.
- Tests: added regression coverage and re-verified the repo with fmt/test/vet/build.

## Affected Flows

- Runtime flow: Codex authentication, refresh, request routing, and `/v1/models` visibility.
- User/API flow: operator-facing `auth codex`, `status`, and `refresh --provider codex` behavior.
- Background/deployment/dev workflow: OpenSpec change tracking and repo-local agent instructions. No deployment pipeline files changed, but pushing `main` would deploy, so this change stays on a feature branch for PR review.

## Impact And Risks

- Impact: completes the Codex-provider implementation and removes secret leakage from config/status/logging.
- Risk: auth/storage behavior changed in a sensitive area; mitigated by regression tests plus full `make test`, `make vet`, and `make build` verification.
- Mitigation/notes: explicit model routing now fails closed on capability mismatch instead of silently falling back.

## Untracked Files

- `AGENTS.md`: staged.
- `openspec/config.yaml`: staged.
- `openspec/changes/add-codex-provider/.openspec.yaml`: staged.
- `openspec/changes/add-codex-provider/proposal.md`: staged.
- `openspec/changes/add-codex-provider/design.md`: staged.
- `openspec/changes/add-codex-provider/specs/codex-provider-support/spec.md`: staged.
- `openspec/changes/add-codex-provider/specs/multi-provider-routing/spec.md`: staged.
- `openspec/changes/add-codex-provider/tasks.md`: staged.
- `.omo/`: excluded from commit as local OpenCode runtime state.
- `.serena/`: excluded from commit as local Serena memory/state.
- `.opencode/`: excluded from commit in this PR because it appears to be local/generated workflow tooling state rather than part of the requested Codex implementation.

## Sensitive/Risk Review

- Secrets checked: no auth tokens, API keys, refresh tokens, or account IDs are included in the staged diff.
- Large/generated files: excluded `.omo/`, `.serena/`, and `.opencode/` local tool-state directories from the commit.
- Deleted files: none.

## Verification

```bash
make fmt
go test ./src/... -run 'TestResolveCodexChatGPTAuthPrefersOfficialStore|TestResolveCodexChatGPTAuthFallsBackToConfig|TestResolveCodexAPIKeyPrefersEnvOverConfig|TestModelRouterResolve_AmbiguousModelRequiresRule|TestModelRouterResolve_ExplicitModelNeedsMatchingCapability|TestModelsEndpointKeepsModelMapVisibleWhenProviderUnavailable'
make test
make vet
make build
```

Result:

```text
make fmt: passed
targeted regression tests: passed
make test: passed
make vet: passed
make build: passed
lsp_diagnostics/gopls: unavailable in this environment
```

## Commit Comments

Complete Codex provider support beside Copilot.

- prefer official Codex auth store for ChatGPT-backed credentials
- keep proxy config free of persisted ChatGPT secrets
- sanitize Codex status/logging output
- fail explicit model routing on capability mismatch instead of falling back
- add regression coverage for auth resolution and multi-provider routing
- include AGENTS.md and OpenSpec artifacts for the implemented change

## Notes

- Remaining risk: `.opencode/`, `.omo/`, and `.serena/` remain untracked locally and are intentionally excluded from this PR.
