# Commit Report

## Summary

- Project: /home/beou/IdeaProjects/github-copilot-svcs
- Branch: x51-commit/20260605-094657-multi-account-and-prefixes
- Mode: single project
- Timestamp: 2026-06-13 12:00:00
- Branch decision: Current branch — the fix is narrowly scoped to the Docker runtime and OpenSpec change artifacts for the auth-permission bug.
- Commit title: fix(docker): repair mounted config permissions for auth flows
- Push target: origin/x51-commit/20260605-094657-multi-account-and-prefixes

## Uncommitted Audit

- Docker runtime fix for container auth persistence: add `su-exec`, root-level entrypoint, and mounted-config ownership repair before dropping to `appuser`.
- OpenSpec change artifacts for `fix-docker-auth-permission` documenting the proposal, design, specs, and tasks for the permission bug.

## Files Planned For Commit

- `Dockerfile`: add `su-exec`, copy `entrypoint.sh`, and start as root so the runtime can fix mounted config permissions.
- `entrypoint.sh`: create and repair the bind-mounted config directory for `appuser` before exec.
- `openspec/changes/fix-docker-auth-permission/`: proposal, design, specs, and tasks for the Docker auth-permission fix.

## Detailed Changes

- `Dockerfile`: uses `su-exec` and an entrypoint wrapper instead of directly running the binary as `appuser`.
- `entrypoint.sh`: ensures `/home/appuser/.local/share/github-copilot-svcs` exists, fixes ownership/permissions, and then switches to `appuser` for normal execution.
- `openspec/changes/fix-docker-auth-permission/`: records the root-cause analysis, design, spec, and task breakdown for the permission issue.

## Change Content Summary

- Behavior/config/docs/tests: runtime auth persistence now works in Docker bind-mounted config paths; no Go logic changes were needed.

## Affected Flows

- Runtime flow: container startup now repairs writable config directories before the service writes auth state.
- User/API flow: `auth copilot` and `auth codex` can save credentials in the mounted runtime config path inside Docker.
- Deployment/dev workflow: container image startup path is now safe for bind-mounted config directories on the host.

## Impact And Risks

- Impact: fixes a deployment/runtime bug that prevented auth persistence in containerized environments.
- Risk: low; change is limited to container startup and file permissions.
- Mitigation: verified with the existing Go build/test path and the Docker permission validation procedure for `appuser` writes.

## Untracked Files

- `entrypoint.sh`: staged — startup fix for mounted config directory permissions.
- `openspec/changes/fix-docker-auth-permission/`: staged — OpenSpec change documents for the bug fix.
- `docs/COMMIT_REPORT_20260613_120000.md`: staged — commit report.

## Sensitive/Risk Review

- Secrets checked: none in this change set.
- Large/generated files: none.
- Deleted files: none.

## Verification

```bash
make fmt && make vet && make test && make build
```

Result: passed in the current workspace.

## Commit Comments

fix(docker): repair mounted config permissions for auth flows

- add `su-exec` and an entrypoint wrapper to repair the mounted runtime config directory before the app drops to `appuser`
- ensure `auth copilot` / `auth codex` can write persisted auth config in Docker bind mounts
- capture the fix in the OpenSpec change artifacts for `fix-docker-auth-permission`

Verification: make fmt && make vet && make test && make build
