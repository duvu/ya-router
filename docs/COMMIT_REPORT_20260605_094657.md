# Commit Report

## Summary

- Project: /home/beou/IdeaProjects/github-copilot-svcs
- Branch: x51-commit/20260605-094657-multi-account-and-prefixes
- Mode: single project
- Timestamp: 2026-06-05 09:46:57
- Branch decision: New branch — large diff (1161 insertions), spans auth/security/routing/tests/docs/provider code, plus untracked OpenSpec changes and new source files
- Commit title: feat(multi-account): copilot+codex account pools, gc-/oc- model prefixes
- Push target: origin/x51-commit/20260605-094657-multi-account-and-prefixes

## Uncommitted Audit

- **Copilot multi-account pool**: `CopilotAccount` struct, `Accounts []CopilotAccount`, cooldown helpers, `advanceAccount()`, failover loop in `ProxyFreeChatRequest`, CLI `--account` flag, pool-aware status/refresh
- **Codex multi-account pool**: `CodexAccount` struct, `Accounts []CodexAccount`, cooldown helpers, `advanceCodexAccount()`, failover loop in `ProxyRequest`, `proxyExecutor` hook, pool-aware CLI auth/status/refresh
- **Provider model prefixes**: `gc-` for Copilot, `oc-` for Codex — `src/prefixes.go` (NEW), prefix injection in `modelsHandler`, prefix-aware routing in `Resolve()`
- **Config schema**: `normalizeCodexAccounts`, `normalizeCopilotAccounts`, backward-compat promotion of legacy `auth` to `accounts[0]`
- **Config example + docs**: `config.example.json` updated with multi-account fields for both providers; `docs/CONFIGURATION.md` (NEW) comprehensive operator guide
- **Tests**: 20+ new tests covering pool/failover/prefix/routing behavior
- **OpenSpec artifacts**: `codex-multi-account`, `copilot-multi-account`, `provider-model-prefixes`, `rollback-rust-and-open-models`, `port-project-to-rust`, `rust-complete-port` changes

## Files Planned For Commit

**Tracked modified:**
- `config.example.json`: multi-account fields for copilot and codex
- `src/cli.go`: pool-aware auth/status/refresh for both providers
- `src/codex_provider.go`: account pool cursor, failover loop, proxyExecutor hook
- `src/config.go`: CodexAccount struct, normalizeCodexAccounts, AccountCooldownSeconds
- `src/config_test.go`: updated 3 tests to read tokens from Accounts[0].Auth
- `src/copilot_provider.go`: account pool cursor, advanceAccount, isAccountLimitSignal, failover loop
- `src/integration_test.go`: 20+ new pool/failover/prefix/routing tests
- `src/main.go`: --account flag for both providers' auth commands
- `src/models.go`: prefix injection in modelsHandler, prefix-tolerant filterAllowedModels
- `src/proxy_test.go`: updated test to assert gc-gpt-4/oc-o1-preview
- `src/router.go`: prefix-aware Resolve, model_map bare fallback, resolveWithPrefix

**Untracked (new):**
- `src/prefixes.go`: prefix constants and helpers (gc-/oc-)
- `src/prefixes_test.go`: unit tests for prefix helpers
- `docs/CONFIGURATION.md`: comprehensive operator config guide
- `openspec/changes/codex-multi-account/`: proposal, design, specs, tasks (29 tasks)
- `openspec/changes/copilot-multi-account/`: proposal, design, specs, tasks (19 tasks)
- `openspec/changes/provider-model-prefixes/`: proposal, design, specs, tasks (21 tasks)
- `openspec/changes/rollback-rust-and-open-models/`: proposal, design, specs, tasks (12 tasks)
- `openspec/changes/port-project-to-rust/`: proposal, design, specs, inventory, tasks (12 tasks)
- `openspec/changes/rust-complete-port/`: proposal, design, specs, tasks (36 tasks)

**Excluded (local tool state):**
- `.omo/`, `.opencode/`, `.serena/` — local agent/session state, never committed

## Detailed Changes

- `src/prefixes.go`: NEW — `CopilotModelPrefix="gc-"`, `CodexModelPrefix="oc-"`, `ProviderPrefix`, `ProviderOwnedBy`, `AddModelPrefix`, `StripModelPrefix`
- `src/models.go`: `modelsHandler` applies prefix on each model, derives `OwnedBy` from provider; `isModelAllowedWithPrefix` tolerates both bare and prefixed allowed_models entries
- `src/router.go`: `Resolve()` strips prefix before lookup; model_map full then bare key fallback; `resolveWithPrefix()` routes to identified provider's catalog; improved ambiguity error with prefix examples
- `src/copilot_provider.go`: `CopilotProvider` gains `accountCursor`, `activeAccount()`, `authState()` pool-aware, cooldown helpers, `advanceAccount()`, `isAccountLimitSignal()` (package-level), `ProxyFreeChatRequest` outer account loop; test hooks `freeModelResolver` and `proxyExecutor`
- `src/codex_provider.go`: `CodexProvider` gains `accountCursor`, `activeAccount()`, `authState()` pool-aware, `codexCooldownSeconds()`, `isCodexAccountInCooldown()`, `firstHealthyCodexAccount()`, `advanceCodexAccount()`, `ProxyRequest` outer account loop; test hook `proxyExecutor`
- `src/config.go`: `CopilotAccount{Label, Auth, LastLimitedAt}`, `Accounts []CopilotAccount`, `AccountCooldownSeconds` (default 300) in `CopilotProviderConfig`; `normalizeCopilotAccounts` promotes legacy `Auth.GitHubToken`; `CodexAccount{Label, Auth, LastLimitedAt}`, `Accounts []CodexAccount`, `AccountCooldownSeconds` (default 300) in `CodexProviderConfig`; `normalizeCodexAccounts` promotes legacy credentials
- `src/cli.go`: `handleAuthCopilot(mode, label)`, `resolveOrCreateCopilotAccount`, `handleAuthCodex(label)`, `resolveOrCreateCodexAccount`, `printCopilotStatus` and `printCodexStatus` pool-aware tables, `refreshCopilot`/`refreshCodex` pool iteration
- `src/main.go`: `--account` flag plumbed through to both providers' auth handlers
- `config.example.json`: `accounts: []` and `account_cooldown_seconds: 300` for both providers
- `docs/CONFIGURATION.md`: Copilot setup, Copilot multi-account, Codex setup, Codex multi-account, model prefixes, routing/model_map, allowed_models note, timeouts, troubleshooting

## Change Content Summary

- **Behavioral**: `/v1/models` now returns `gc-`/`oc-` prefixed model IDs; prefix-aware routing; Copilot and Codex both support account-pool failover on 429/rate-limit-403
- **Config**: `accounts` array + `account_cooldown_seconds` for both providers; backward-compat auto-promotion of legacy `auth` field
- **Tests**: 20+ new tests for pool normalization, cursor advance, cooldown, failover, prefix helpers, routing edge cases
- **Docs**: Full operator configuration guide at `docs/CONFIGURATION.md`

## Affected Flows

- Runtime flow: `/v1/models` response shape changes (prefixed IDs); chat/embeddings routing strips prefix before upstream; Copilot and Codex chat requests may auto-advance to next pool account on rate limit
- User/API flow: clients sending bare model IDs still work (backward compat); prefixed IDs (`gc-*`, `oc-*`) enable deterministic provider targeting
- Background/deployment/dev workflow: auth commands now accept `--account` label for both providers; status shows per-account pool when 2+ accounts

## Impact And Risks

- Impact: BREAKING for `/v1/models` consumers expecting bare model IDs without prefixes; bare IDs still accepted by the router so requests continue to work
- Risk: Auth/credential code touched for both providers — tested but `gopls` unavailable for static analysis
- Mitigation: `make fmt && make vet && make test && make build` all passed; 20+ targeted regression tests for new behavior; pre-existing tests updated

## Untracked Files

- `src/prefixes.go`: staged — new source file implementing model prefix helpers
- `src/prefixes_test.go`: staged — tests for prefix helpers
- `docs/CONFIGURATION.md`: staged — new operator guide
- `openspec/changes/*/`: staged — OpenSpec change artifacts (proposals, designs, specs, tasks)

## Sensitive/Risk Review

- Secrets checked: no tokens/keys in diff; `clearPersistedChatGPTSecrets` / `resolveOrCreateCodexAccount` patterns keep secrets in pool auth state only
- Large/generated files: none
- Deleted files: none in this commit

## Verification

```bash
make fmt && make vet && make test && make build
```

Result:

```text
go fmt ./src/...
go vet ./src/...
go test ./src/...
ok  github.com/x51vn/github-copilot-svcs/src  5.299s
go build -ldflags="-s -w -X main.version=dev" -o github-copilot-svcs ./src
```

## Commit Comments

feat(multi-account): copilot+codex account pools, gc-/oc- model prefixes

- Provider model prefixes: gc- for Copilot models, oc- for Codex models
  - src/prefixes.go: prefix constants + AddModelPrefix/StripModelPrefix helpers
  - src/models.go: prefix injection in modelsHandler; prefix-tolerant allowed_models
  - src/router.go: prefix-aware Resolve, model_map bare fallback, resolveWithPrefix
- Copilot multi-account pool (account_cooldown_seconds=300)
  - CopilotAccount struct, Accounts field in CopilotProviderConfig
  - normalizeCopilotAccounts promotes legacy auth.github_token to accounts[0]
  - advanceAccount() with cooldown window on 429/rate-limit-403
- Codex multi-account pool (account_cooldown_seconds=300)
  - CodexAccount struct, Accounts field in CodexProviderConfig
  - normalizeCodexAccounts promotes legacy credentials to accounts[0]
  - advanceCodexAccount() with cooldown window on 429/rate-limit-403
  - proxyExecutor hook for test interception (same pattern as Copilot)
- CLI: --account flag for auth copilot and auth codex
- CLI: per-account pool status display (2+ accounts)
- CLI: refreshCopilot/refreshCodex iterate pool accounts
- 20+ new tests: pool normalization, cursor advance, cooldown, failover, prefix helpers
- config.example.json: accounts/cooldown fields for both providers
- docs/CONFIGURATION.md: new comprehensive operator guide

Verification: make fmt && make vet && make test (ok) && make build

## Notes

- Remaining risk: gopls not installed; static type analysis not available for this repo
- /v1/models now returns prefixed IDs — clients expecting bare IDs from this service need to be updated or use bare IDs in model_map for routing
